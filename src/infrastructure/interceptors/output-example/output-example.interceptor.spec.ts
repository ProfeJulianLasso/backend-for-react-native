import { OutputExampleInterceptor } from './output-example.interceptor';

describe('OutputExampleInterceptor', () => {
  it('should be defined', () => {
    expect(new OutputExampleInterceptor()).toBeDefined();
  });
});
