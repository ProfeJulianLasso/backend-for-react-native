import { InputExampleInterceptor } from './input-example.interceptor';

describe('InputExampleInterceptor', () => {
  it('should be defined', () => {
    expect(new InputExampleInterceptor()).toBeDefined();
  });
});
